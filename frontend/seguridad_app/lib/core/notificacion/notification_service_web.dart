class NotificationService {
  static Future<void> initialize() async {
    // no-op en web
  }

  static Future<void> showAlarmaPuerta(String title, String body) async {
    // no-op en web
    // la alerta web la manejás con UI (dialog / overlay)
  }
}
